<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
       （省略）
        @livewireStyles
    </head>
    <body class="font-sans text-gray-900 antialiased">
       （省略）
        @livewireScripts
    </body>
</html>